# Navigate to the backend directory
cd "d:\Project\All backend Project\Digital-Label\backend"

# Install Composer dependencies (if you haven't recently)
composer install

# Start the Laravel local development server (Runs at http://127.0.0.1:8000)
php artisan serve



# Navigate to the frontend directory
cd "d:\Project\All backend Project\Digital-Label\frontend"

# Install NPM dependencies (if needed)
npm install

# Start the Next.js development server with Turbopack (Runs at http://localhost:3000)
npm run dev


kitzuadmin@gmail.com
kitzuadmin9080@@



db pw = kitzulabel@@99!

$user = App\Models\User::where('role', 'admin')->first();
$user->email = 'new-email@gmail.com';
$user->password = Hash::make('new-password-here');
$user->save();


then run 
php artisan tinker


Type exit and press Enter to leave. Done!


git add . && git commit -m "update code" && git push origin main && git push https://github.com/Heang0/Digital-Label.git main
